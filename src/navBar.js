import React, { Component } from 'react';
import ReactDOM from 'react-dom';
import './navBar.css';

const element = (
	<div>

	</div>
	);
ReactDOM.render(
  element,
  document.getElementById('navBar')
);
